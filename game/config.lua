local Screen = require('')
local config = {
  debug = false,
  levelSpeed = 0,
  xMatrix = 7,
  yMatrix = 7,
  tiles = 6,
  columnXPositions = {}
}


config.setXPositions = function()

  for i = 0, config.tiles, 1 do
    
  end
end

config.setXPositions()

return config